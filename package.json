{
  "description": "NativeScript Application",
  "license": "SEE LICENSE IN <your-license-filename>",
  "readme": "NativeScript Application",
  "repository": "<fill-your-repository-here>",
  "nativescript": {
    "id": "org.nativescript.JSONENVTest",
    "tns-ios": {
      "version": "3.4.0"
    },
    "hooks": [
      {
        "type": "after-prepare",
        "script": "after-prepare.js"
      }
    ]
  },
  "scripts": {
    "run-dev-ios": "npm run clean && rm -r app/config && cross-env BUILD_PROFILE=dev tns run ios ",
    "run-prod-ios": "npm run clean && rm -r app/config && cross-env BUILD_PROFILE=prod tns run ios",
    "build-prod-ios": "cross-env BUILD_PROFILE=prod tns build ios --clean"
  },
  "dependencies": {
    "nativescript-theme-core": "~1.0.4",
    "tns-core-modules": "~3.4.0"
  },
  "devDependencies": {
    "cross-env": "^5.1.3",
    "mkdirp": "^0.5.1",
    "nativescript-dev-typescript": "~0.6.0",
    "typescript": "~2.4.2"
  }
}
